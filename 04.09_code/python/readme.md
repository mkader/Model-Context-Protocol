* Set up virtual environment
    ```sh
    python -m venv venv
    source ./venv/bin/activate
    ```
* Install dependencies

    ```
    pip install "mcp[cli]"

    python client.py

    ```